<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.task_manager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskManager">
        <activity android:name=".FilterTasks"></activity>
        <activity android:name=".ChooseDates" />
        <activity android:name=".Chart" />
        <activity android:name=".EmployeePanel" />
        <activity android:name=".ManagerPanel" />
        <activity android:name=".PostRole"
            android:parentActivityName=".AdminPanelOne"/>
        <activity android:name=".UserInfo" />
        <activity android:name=".TaskInfo" />
        <activity android:name=".PostTask"
            android:parentActivityName=".AdminPanelOne"/>
        <activity android:name=".PostUser"
            android:parentActivityName=".AdminPanelOne"/>
        <activity android:name=".AdminPanelOne"/>
        <activity android:name=".EmployeePanel1" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateAlwaysVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>